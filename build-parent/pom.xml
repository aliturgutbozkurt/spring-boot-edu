<?xml version="1.0" encoding="UTF-8"?>
<!--
  Parent POM for every course module (lesson / exercise / solution) and the capstone.
  All versions live here — modules never declare versions that a BOM already manages.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.springbootedu</groupId>
    <artifactId>build-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Spring Boot Edu :: Build Parent</name>

    <properties>
        <java.version>27</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Release trains that are not part of the Spring Boot BOM -->
        <spring-cloud.version>2025.1.3</spring-cloud.version>
        <spring-ai.version>2.0.1</spring-ai.version>
        <spring-modulith.version>2.1.1</spring-modulith.version>
        <!-- Not in the Boot BOM; 3.1.x is built against Spring Boot 4.1 -->
        <springdoc.version>3.1.1</springdoc.version>
        <!-- Not in the Boot BOM; the standalone jar shades its own Jetty -->
        <wiremock.version>3.13.2</wiremock.version>
        <!-- Not in the Boot BOM; 1.5.0 reads Java 27 class files (checked 2026-09-24) -->
        <archunit.version>1.5.0</archunit.version>

        <!-- JDK 21+ warns when Mockito/Byte Buddy attach their agent at runtime; allow it explicitly in tests -->
        <argLine>-XX:+EnableDynamicAgentLoading</argLine>

        <!-- Course artifacts are never published -->
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.wiremock</groupId>
                <artifactId>wiremock-standalone</artifactId>
                <version>${wiremock.version}</version>
            </dependency>
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.modulith</groupId>
                <artifactId>spring-modulith-bom</artifactId>
                <version>${spring-modulith.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <!-- Lesson code must not use deprecated/removed APIs (SPEC success criterion 7).
                             An explicit "old vs new" example opts out with @SuppressWarnings. -->
                        <compilerArgs>
                            <arg>-Xlint:deprecation,removal,unchecked</arg>
                        </compilerArgs>
                        <failOnWarning>true</failOnWarning>
                        <!-- JDK 23+ runs annotation processors only when configured explicitly.
                             The configuration processor generates metadata for @ConfigurationProperties
                             (IDE auto-completion in application.yaml). Versions come from the Boot BOM. -->
                        <annotationProcessorPathsUseDepMgmt>true</annotationProcessorPathsUseDepMgmt>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.springframework.boot</groupId>
                                <artifactId>spring-boot-configuration-processor</artifactId>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <executions>
                    <execution>
                        <id>enforce-course-rules</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[27,)</version>
                                    <message>This course needs JDK 27. Run: export JAVA_HOME=$(/usr/libexec/java_home -v 27)  (Linux/Windows: point JAVA_HOME to a JDK 27)</message>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9,)</version>
                                    <message>Use the Maven wrapper: ./mvnw</message>
                                </requireMavenVersion>
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>org.projectlombok:lombok</exclude>
                                    </excludes>
                                    <message>Lombok is not used in this course — use records and plain Java (see CLAUDE.md).</message>
                                </bannedDependencies>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- *Test → Surefire (test phase), *IT → Failsafe (integration-test/verify phases) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Projects without Java sources (this parent, pom-only starters) are not applications.
             Lets "./mvnw -pl <lesson> -am spring-boot:run" run the lesson and skip the rest. -->
        <profile>
            <id>not-an-application</id>
            <activation>
                <file>
                    <missing>${basedir}/src/main/java</missing>
                </file>
            </activation>
            <properties>
                <spring-boot.run.skip>true</spring-boot.run.skip>
            </properties>
        </profile>
    </profiles>
</project>
