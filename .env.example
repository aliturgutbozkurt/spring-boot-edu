# Copy to .env to override the local development defaults used by compose.yaml.
# .env is git-ignored. Never put real credentials here.
POSTGRES_DB=bookstore
POSTGRES_USER=bookstore
POSTGRES_PASSWORD=bookstore
