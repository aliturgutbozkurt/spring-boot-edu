# Tests: every cached Spring context runs its own Kafka Streams instance in this JVM.
# Each needs its own local state directory, or the second one fails with "Unable to initialize state".
spring.kafka.streams.state-dir=${java.io.tmpdir}/kafka-streams-test/${random.uuid}
