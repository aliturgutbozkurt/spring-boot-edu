/**
 * Exercise 3 — given: a small shop in layers ({@code shop}) and an old module that breaks the rules ({@code legacy}).
 */
@NullMarked
package com.springbootedu.testing.exercise3;

import org.jspecify.annotations.NullMarked;
